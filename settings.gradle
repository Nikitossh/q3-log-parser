pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
      // id 'io.quarkus' version "${quarkusPluginVersion}"
      id 'io.quarkus' version '3.20.2'
    }
}

rootProject.name = 'quake3-logger'
